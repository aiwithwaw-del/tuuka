name: tuuka
description: "Tuuka - Professional Challenge & Reward Platform"
publish_to: 'none'
version: 3.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  intl: ^0.18.1
  flutter_local_notifications: ^16.1.0
  url_launcher: ^6.2.1
  image_picker: ^1.0.4
  geolocator: ^10.1.0
  path_provider: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/